import React from 'react';
import 'whatwg-fetch';

import {
  Button,
  ButtonArea,
  CellBody,
  CellFooter,
  CellHeader,
  CellsTitle,
  Form,
  FormCell,
  Slider,
  Switch,
} from 'react-weui';

export default class App extends React.PureComponent {
	static dateConfig = {
		month: 'long',
		day: 'numeric',
	}
}